# 4 MB flash, A/B OTA, 512 kB LittleFS.
#
# There is deliberately no `factory` partition. With A/B slots a factory image is
# a third copy of the firmware that is never the one running, and on 4 MB that
# third costs more than it is worth. `idf.py flash` writes ota_0.
#
# The consequence: the bootloader follows `otadata`, so after an OTA the device
# boots ota_1 and a subsequent `idf.py flash` would write ota_0 and still boot the
# old image. tools/flash_a.py erases otadata as part of flashing, which is what
# makes manual flashing mean what it appears to mean.
#
# Name,   Type, SubType,  Offset,   Size,     Flags
nvs,      data, nvs,      0x9000,   0x4000,
otadata,  data, ota,      0xd000,   0x2000,
phy_init, data, phy,      0xf000,   0x1000,
ota_0,    app,  ota_0,    0x10000,  0x1B0000,
ota_1,    app,  ota_1,    0x1C0000, 0x1B0000,
res,      data, littlefs, 0x370000, 0x80000,
